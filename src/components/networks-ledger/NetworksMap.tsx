/**
 * Named map pins were removed under the privacy-first Terror & Finance redesign.
 * Geographic patterns now render as aggregate region charts on the public page.
 * This module is kept only so old deep imports do not break builds.
 */
export function NetworksMap() {
  return null;
}
